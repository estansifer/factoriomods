-- require('abridged_auto')

-- enable_all(without_prefix('recipe', 'ab-'), false)
-- enable_all(with_prefix('recipe', 'ab-'), true)

-- enable_all(without_prefix('technology', 'ab-'), false)
-- enable_all(with_prefix('technology', 'ab-'), true)

-- For compatibility with SpaceEx we have to disable techs from control.lua.
-- Otherwise its integrity check complains.
